/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If your game is in a subdirectory, e.g., 'src', you might need to configure webpack
  // to correctly resolve modules, or adjust your import paths.
  // For now, this is a minimal config.
};

module.exports = nextConfig;
